  <title>Editar perfil</title>
</head>

<body style="background-color:#F1F1F1">
  <%= render "partials/banner" %>
  <div class="container" style="background-color:white; padding-top:2%; padding-left: 5%; padding-right: 5%; padding-bottom: 5%">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <div class="row">
      <h3><strong>EDITAR PERFIL</strong></h3>
      <hr>
      <div class="col-md-6" style="margin-top:3%">
        <div class="thumbnail" style="width:80%">
          <h4>Foto de perfil</h4>
          <img style="width:100%; height:300px" src="https://ae01.alicdn.com/kf/HTB1DEWoHVXXXXclXFXXq6xXFXXX3/M-sica-moda-pared-del-vinilo-tambores-instrumentos-musicales-m-sica-Rock-Pop-Mural-etiqueta-de.jpg">
        </div>
      </div>
      <div class="col-md-6" style="margin-top:3%">
        <div class="input-group">
          <span class="input-group-addon"><i class="glyphicon glyphicon-user"></i></span>
          <%= f.text_field :name, autofocus: true, value: "#{current_user.name}", class: "form-control", placeholder:"Nombre" %>
        </div>
        <div class="input-group" style="margin-top:3%">
          <span class="input-group-addon"><i class="glyphicon glyphicon-envelope"></i></span>
          <%= f.email_field :email, autofocus: true, class: "form-control", placeholder:"Correo" %>
        </div>

        <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
          <div>Currently waiting confirmation for:
            <%= resource.unconfirmed_email %>
          </div>
        <% end %>
        <div class="input-group" style="margin-top:8%">
          <span class="input-group-addon"><i class="glyphicon glyphicon-refresh"></i></span>
          <%= f.password_field :password, autocomplete: "off",  class: "form-control", placeholder:"Nueva contraseña" %>
        </div>
        <i style="color:grey; font-size:12px;">(Déjalo en blanco si no lo quieres editar)</i>
        <% if @minimum_password_length %>
          <em style="font-size:12px; color:#CF4232">Mínimo <%= @minimum_password_length %> caracteres</em>
        <% end %>

        <div class="input-group" style="margin-top:3%">
          <span class="input-group-addon"><i class="glyphicon glyphicon-refresh"></i></span>
          <%= f.password_field :password_confirmation, autocomplete: "off",  class: "form-control", placeholder:"Confirmar nueva contraseña" %>
        </div>
        <div class="input-group" style="margin-top:8%">
          <span class="input-group-addon"><i class="glyphicon glyphicon-lock"></i></span>
          <%= f.password_field :current_password, autocomplete: "off",  class: "form-control", placeholder:"Contraseña actual" %>
        </div>
        <div class="input-group" style="margin-top:8%">
          <span class="input-group-addon"><i class="glyphicon glyphicon-globe"></i></span>
          <%= f.text_field :country, autocomplete: "off",  class: "form-control", placeholder:"Contraseña actual" %>
        </div>

      </div>
    </div>

    <div class="row" style="margin-top:3%">
      <div class="col-md-2">
          <%= f.submit "Guardar", class:"btn btn-success btn-block" %>
          <br>
          <%= button_to "Cancelar cuenta", registration_path(resource_name), data: { confirm: "¿Seguro que deseas cancelar tu cuenta?" }, method: :delete, class:"btn btn-danger btn-block"  %>

      </div>
    </div>
    <% end %>
  </div>
  <%= render "partials/footer" %>
</body>
